# A+B 组合选品系统 — 傻瓜式部署指南
# 把这个文档发给买家，对方照着做就行

---

## 你只需要准备两样东西

1. **一台 Windows 电脑**（Win10 或 Win11，8GB 内存以上）
2. **OpenAI 的 API Key**（需要付费账号，不是免费版）

---

## 第一步：下载安装包

把发给你的整个文件夹放到电脑上，路径不要有中文。

比如：`D:\bundling-system`

---

## 第二步：安装 PostgreSQL（数据库）

> 这个得手动装，因为自动安装容易失败。

1. 打开 https://www.postgresql.org/download/windows/
2. 点绿色按钮 **Download the installer**
3. 选择最新版的 Windows x86-64，下载
4. 双击运行安装包
5. 一路 **Next**，到设置密码那一步，**密码输入：bundling**
6. 端口保持默认 **5432**
7. 装完后重启电脑

---

## 第三步：安装 Redis（任务队列）

按 `Win` 键，搜索 **PowerShell**，右键"以管理员身份运行"，粘贴下面这行回车：

```powershell
winget install -e --id Microsoft.MicrosoftRedis --accept-source-agreements --silent
```

装好后运行一次启动 Redis 服务：

```powershell
Start-Service Redis
```

---

## 第四步：一键安装其余所有东西

还是在刚才的管理员 PowerShell 里，粘贴下面一整段跑：

```powershell
# 安装 Python 3.12
winget install -e --id Python.Python.3.12 --accept-source-agreements --silent

# 安装 Node.js 20
winget install -e --id OpenJS.NodeJS.LTS --accept-source-agreements --silent
```

关掉 PowerShell，重新打开一次（**必须是管理员**），然后进到项目目录：

```powershell
cd D:\bundling-system

# 创建虚拟环境
python -m venv .venv

# 装 Python 依赖
.venv\Scripts\pip install -r requirements.txt
.venv\Scripts\pip install -r backend\requirements.txt

# 装浏览器（用于爬虫）
.venv\Scripts\python -m playwright install chromium

# 装前端依赖
npm --prefix frontend install

# 创建配置文件
Copy-Item .env.example .env
Copy-Item backend\.env.example backend\.env

# 初始化数据库
.venv\Scripts\python -m alembic -c backend\migrations\alembic.ini upgrade head
```

看到最后一行输出 `OK` 说明数据库初始化成功。

---

## 第五步：配置 API Key

打开项目目录里的 `.env` 文件（用记事本打开就行），找到这一行：

```
OPENAI_API_KEY=sk-your-key-here
```

改成你的真实 Key。如果 Key 是 `sk-proj-xxxx` 那就改成：

```
OPENAI_API_KEY=sk-proj-xxxx
```

保存文件。

---

## 第六步：启动系统

项目目录里有个 **启动.ps1** 文件，右键"使用 PowerShell 运行"。

或者手动在 PowerShell 里：

```powershell
cd D:\bundling-system

# 打开终端 1 — 后端 API
python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000
```

**保持这个窗口开着**，再开一个新的管理员 PowerShell：

```powershell
cd D:\bundling-system

# 打开终端 2 — 任务 Worker
.venv\Scripts\python -m arq backend.workers.settings.WorkerSettings
```

**也保持开着**，再开第三个 PowerShell：

```powershell
cd D:\bundling-system

# 打开终端 3 — 前端界面
npm --prefix frontend run dev
```

---

## 第七步：开始使用

打开浏览器访问：**http://localhost:3000**

看到界面就成功了！点"假设分析"，输入一个 Walmart 商品链接试试。

---

## 常见问题

**Q: 启动时说 Python 不是命令？**
重新打开 PowerShell 再试，或者重启电脑。

**Q: PostgreSQL 连不上？**
检查 PostgreSQL 有没有启动：搜索"Services"（服务），找到 PostgreSQL，右键启动。

**Q: `alembic upgrade head` 报错说连不上数据库？**
检查 PostgreSQL 是否在运行，密码是不是 `bundling`。

**Q: 提交任务后一直"排队中"不动？**
检查 Worker 那个窗口有没有报错，Redis 是不是启动了。

**Q: 哪里能看我的 OpenAI 花了多少钱？**
https://platform.openai.com/usage
