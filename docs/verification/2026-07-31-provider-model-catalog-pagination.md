# API 模型目录分页验证

## 优化内容

- 模型目录固定每页显示 10 个模型。
- 模型超过 10 个时显示“上一页 / 第 X / Y 页 / 下一页”。
- 切换供应商、重新测试连接或连接参数失效时回到第 1 页。
- 默认模型选择和配置保存逻辑保持不变。

## TDD 验证

- RED：21 个模型时，第 11 个模型仍出现在第一页，分页测试按预期失败。
- GREEN：实现分页后，第一页显示 1–10，第二页显示 11–20，分页测试通过。

## 回归结果

- `npm test -- --run tests/provider-settings.test.tsx`：12 项通过。
- `npm test -- --run`：19 个测试文件、147 项测试通过。
- `npm run build`：Next.js 生产构建成功。

## 已知非本次问题

- 完整测试仍输出部分既有 MSW 未匹配 `/api/v1/settings/providers` 请求警告，但测试均通过，本次没有新增失败。
