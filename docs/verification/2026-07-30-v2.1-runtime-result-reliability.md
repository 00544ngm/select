# V2.1 运行与结果可靠性验收

## 验收结论

- 验收日期：2026-07-30
- 分支：`codex/stickiness-scoring-v2-1`
- 代码基线：`2295c7f` (`feat: expose v2.1 result reliability`)
- 运行目录：`C:\Users\Administrator\Documents\组合品\web-platform-v2.1-work`
- 结论：V2.1 运行契约、真实自定义 Anthropic 任务、API/JSON/Excel/页面结果一致性通过。CatToken 单次连接测试仍返回上游 503，因此不能宣称 CatToken 当前可用。

## 自动化验证

| 检查 | 结果 |
| --- | --- |
| Python 全量测试 | `389 passed`；中文路径下首次日志重定向出现 8 个 GBK 子进程解码警告，另有 1 个既有异步连接清理警告；使用 `PYTHONUTF8=1` 复核 |
| 前端全量测试 | `116 passed` |
| TypeScript | 通过 |
| Next.js 生产构建 | 通过；包含 `/`、`/history`、`/jobs/[jobId]`、`/results`、`/settings/api` |
| Task 4 重启脚本测试 | `4 passed` |
| Task 4 改动 Python 文件 Ruff | 通过 |
| 全项目 Ruff | 仍有 87 个既有基线问题；本轮未跨范围批量修复 |
| Git 差异检查 | `git diff --check` 通过 |

TypeScript 与生产构建必须串行：首次并行验证因双方同时读写 `.next` 而失败；随后先执行生产构建、再执行类型检查，二者均以退出码 0 通过。

## 运行握手

项目专用重启脚本完成镜像实例切换后，独立握手得到：

| 项目 | 结果 |
| --- | --- |
| `/health/live` | `ok` |
| 数据库 | `ok` |
| Redis | `ok` |
| Worker | `ok` |
| 运行契约 | `ok` |
| API 模型版本 | `combination_model_v2.1` |
| Worker 模型版本 | `combination_model_v2.1` |
| 可用配置条目 | 4 |
| 首页 | HTTP 200 |
| API 设置页 | HTTP 200 |
| 首页 Next.js 静态资源 | 7 个，全部 HTTP 200 |

API 与 Worker revision 在当前未注入构建 revision 的开发运行环境中均为 `unknown`；两侧值相同且模型契约明确匹配。

## 真实供应商任务

- 任务 ID：`367c230b-41fa-4e03-8f78-09d2cc1483e5`
- 主品：Walmart 商品 `17516068470`（电动盐和胡椒研磨器套装）
- 供应商：`custom`
- 实际模型：`claude-fable-5`
- 模型版本：`combination_model_v2.1`
- 任务状态：`completed`，进度 100，无错误码
- 业务结果：`completed_with_qualified_candidates`
- 初次候选：5
- 遗漏复核候选：0；未触发复核
- 合格：1
- 待补证据：1
- 淘汰：3

候选决策为：

| 候选 | 粘性分 | 执行状态 | 最终动作/原因 |
| --- | ---: | --- | --- |
| 厨房清洁刷套装 | 89 | `pass` | `needs_evidence`；仅有 E1 候选发现证据，不能直接采购 |
| 防水围裙 | 35 | `hold` | 产品类型/关系仍待验证 |
| USB-C 多口充电器 | 59 | `reject` | `duplicate_function` |
| 海盐粗盐补充装 | 100 原始粘性 | `reject` | `food_blocked`，食品过滤后最终分 0 |
| 黑胡椒粒补充装 | 100 原始粘性 | `reject` | `food_blocked`，食品过滤后最终分 0 |

高粘性分不绕过硬门槛。食品候选即使关系紧密也被确定性淘汰；通过关系门槛但证据仅为 E1 的候选仍不可直接采购。

## 四端一致性

| 字段 | API | JSON | Excel `结果摘要` | 任务页面 |
| --- | --- | --- | --- | --- |
| 模型版本 | V2.1 | V2.1 | V2.1 | V2.1 |
| 供应商/模型 | custom / claude-fable-5 | custom / claude-fable-5 | custom / claude-fable-5 | 自定义 Anthropic / claude-fable-5 |
| 业务状态 | 有合格候选 | 有合格候选 | 有合格候选 | 发现合格候选 |
| 初次候选 | 5 | 5 | 5 | 通过分类计数表达 |
| 合格/待补证据/淘汰 | 1 / 1 / 3 | 1 / 1 / 3 | 1 / 1 / 3 | 1 / 1 / 3 |
| 采购语义 | 按动作执行 | 按动作执行 | 不可直接采购 | 页面提示按最终动作执行 |

API 的 `directions` 只保留可展示方向，`structured_directions` 保留全部 5 个候选；JSON 与 Excel 保留全部候选审计明细。这是预期的数据分层，不是计数冲突。

Excel 通过 `@oai/artifact-tool` 结构化读取，包含 `结果摘要`、`商品信息`、`战略判断`、`辅品方向`、`关键词包` 五张工作表；公式错误扫描为 0。

任务页面真实加载主品图片，图片自然尺寸为 2200 x 2200；页面引用 6 个脚本和 1 个样式资源，浏览器控制台无警告或错误。

## CatToken 状态

仅执行一次最小连接测试，结果为 HTTP 503、`PROVIDER_UNAVAILABLE`、可重试。测试没有创建选品任务，也没有修改配置。该结果证明 CatToken 当前不能作为已验收的可用供应商；它不否定 V2.1 应用运行、自定义 Anthropic 端到端任务或结果一致性验收。

## 已知限制与回滚

- 当前活动实例运行在可写镜像目录，原 `F:` 仓库因本会话权限限制尚未同步。
- 自定义 Anthropic 已完成一条真实 V2.1 任务；OpenAI、DeepSeek 和 CatToken 未在本轮分别完成真实选品任务。
- CatToken 恢复后应单独执行一次连接测试，再决定是否进行完整任务验收。
- 全项目 Ruff 仍有 87 个既有问题，需要独立任务清理。
- 回滚 Task 4 时回到提交 `2295c7f`；V2.1 评分和结果可靠性此前各阶段提交保持独立可回滚。
