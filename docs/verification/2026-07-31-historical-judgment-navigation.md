# 历史判定说明跳转验证

日期：2026-07-31

## 实现结果

- `historical_incomplete` 状态从静态标记改为可操作按钮，无障碍名称为“查看历史判定说明”。
- 点击后自动展开当前方向的深度分析，并定位、聚焦和短暂高亮“处理说明”。
- 即使历史方向保存了 `missing_evidence`，历史按钮仍优先定位处理说明；待验证证据继续在下方展示。
- `hold` 状态继续定位具体待验证证据。
- 未修改历史任务结果、分数或原始判断字段。

## TDD 验证

- RED：新增测试后失败，原因是页面不存在“查看历史判定说明”按钮。
- GREEN：实现显式 `evidence | guidance` 定位目标后，新测试通过。

## 自动验证

- `npm.cmd run typecheck`：通过。
- `npm.cmd test -- --run`：19 个测试文件、146 项测试全部通过。
- `npm.cmd run build`：Next.js 生产构建通过。

## 实际页面验证

任务：`0b14fe4f-3459-4fb6-a08c-e9300359befc`

- 页面 HTTP 200。
- 选择“美工刀/可折断刀片工具”历史方向后，“查看历史判定说明”按钮数量为 1。
- 点击后“处理说明”区域数量为 1，且 `document.activeElement` 为该区域、`data-highlighted=true`。
- 处理说明明确展示“为什么、下一步、这意味着什么”。
- “待验证证据”区域仍存在，数量为 1。

