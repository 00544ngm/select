# A+B 组合选品平台详细交接与续作手册

> 最后更新：2026-07-15
>
> 当前实施分支：`feature/web-platform`
>
> 当前实施目录：`D:\Desktop\bundling-system\.worktrees\web-platform`
>
> 原始仓库目录：`D:\Desktop\bundling-system`
>
> 交接原则：保留现有 CLI 功能，在独立的 FastAPI 后端和 Next.js 前端上继续开发。

## 1. 接手者先看这里

后续开发必须在下面的隔离工作树内进行：

```powershell
Set-Location 'D:\Desktop\bundling-system\.worktrees\web-platform'
git status --short --branch
git branch --show-current
```

预期分支为：

```text
feature/web-platform
```

不要在 `main` 分支重新实现，也不要删除当前未提交文件。当前 Task 5 已经先写测试并确认测试失败，这些文件就是下一步开发的起点。

最重要的执行顺序：

1. 完成并提交后端 Task 5。
2. 完成并提交后端 Task 6、7、8。
3. 后端阶段门通过后再创建 Next.js 前端。
4. 按前端计划依次完成 Task 1-8。
5. 最后执行全量测试、构建、Playwright 截图和本地联调。

每个 Task 都必须遵循：先写失败测试，确认 RED，再写最小实现，确认 GREEN，运行 Ruff/类型检查，最后单独提交。

## 2. 项目目标和不可破坏的功能

项目目标是在原有 A+B 组合选品 CLI 核心之外增加前后端分离平台：

- 前端：Next.js、TypeScript、Tailwind CSS、shadcn/ui。
- 后端：FastAPI、SQLAlchemy Async、Alembic、ARQ。
- PostgreSQL：任务、结果、错误、商品快照和产物元数据的权威存储。
- Redis：任务队列、临时进度、锁和可选缓存。
- 原 `app/`：继续作为不依赖 Web 框架的业务核心。

以下 CLI 契约必须一直可用：

```powershell
.venv\Scripts\python.exe -m app.main --mode generate --url '商品链接'
.venv\Scripts\python.exe -m app.main --mode judge --a-url 'A品链接' --b-urls 'B品链接1' 'B品链接2'
.venv\Scripts\python.exe -m app.main --mode batch --input 'urls.txt'
.venv\Scripts\python.exe -m app.main --mode batch --resume '批次ID'
```

架构边界不得破坏：

- `app/` 不得导入 FastAPI、SQLAlchemy、Redis 或前端代码。
- `backend/` 通过适配器调用 `app/`。
- 长时间运行的浏览器和 LLM 工作只能在 ARQ Worker 内执行，不能在 API 请求进程内执行。
- 不得更改现有 JSON/Excel 文件的可读性。
- 不得提交 `.env`、API Key、数据库密码、浏览器资料目录或真实抓取内容。

## 3. 规划、设计和验证文档路径

所有路径都相对于实施目录 `D:\Desktop\bundling-system\.worktrees\web-platform`。

| 用途 | 路径 |
|---|---|
| 总执行规划 | `docs/superpowers/plans/2026-07-15-bundling-platform-master-plan.md` |
| 核心稳定化规划 | `docs/superpowers/plans/2026-07-15-core-stabilization.md` |
| FastAPI 后端规划 | `docs/superpowers/plans/2026-07-15-fastapi-backend.md` |
| Next.js 前端规划 | `docs/superpowers/plans/2026-07-15-nextjs-frontend.md` |
| 总体架构与兼容性设计 | `docs/superpowers/specs/2026-07-15-web-platform-design.md` |
| 已批准的前端布局图 | `docs/design/bundling-dashboard-layout.png` |
| 视觉设计说明 | `docs/design/layout-philosophy.md` |
| 核心阶段验证记录 | `docs/verification/core-stabilization.md` |
| 本交接文档 | `交接文档.md` |

Windows 绝对路径示例：

```text
D:\Desktop\bundling-system\.worktrees\web-platform\docs\superpowers\plans\2026-07-15-fastapi-backend.md
D:\Desktop\bundling-system\.worktrees\web-platform\docs\superpowers\plans\2026-07-15-nextjs-frontend.md
D:\Desktop\bundling-system\.worktrees\web-platform\docs\design\bundling-dashboard-layout.png
```

## 4. Git 状态和已完成提交

仓库已经初始化，实施分支已经有以下关键提交：

```text
266e739 chore: verify stable CLI core
043cde5 feat: scaffold FastAPI backend
1d59952 feat: persist analysis jobs in PostgreSQL
d82c3f7 feat: define versioned job API contracts
8eb6fef feat: enqueue durable analysis jobs
```

当前 HEAD：

```text
8eb6fef feat: enqueue durable analysis jobs
```

当前未提交现场：

```text
 M backend/api/router.py
?? backend/api/dependencies.py
?? backend/api/routes/
?? backend/tests/test_jobs_api.py
```

这些文件属于正在进行的后端 Task 5，必须保留。不要执行下面这些命令：

```text
git reset --hard
git clean -fd
git checkout -- .
```

接手时先查看：

```powershell
git status --short --branch
git diff -- backend/api/router.py
Get-Content -Raw backend/tests/test_jobs_api.py
Get-Content -Raw backend/api/routes/jobs.py
```

## 5. 已完成工作

### 5.1 核心稳定化已完成

已完成并提交：

- 严格校验 Walmart/Amazon URL scheme 和真实 hostname，拒绝仿冒域名。
- 批处理恢复时会重试失败项。
- Checkpoint 使用原子写入。
- 抓取结果缺少必需字段时，在调用 LLM 前拒绝。
- LLM 使用真实 JSON Schema 并验证输出。
- 抓取文本作为不可信数据加入 prompt 分隔边界。
- JSON/Excel 结果文件名防同秒碰撞。
- Excel 可处理空结果和缺失评分。
- 浏览器资源所有权明确，只清理由当前管理器启动的进程。
- CLI generate、judge、batch 兼容性测试存在。

最近一次完整核心验证记录：

```text
77 passed
Ruff: All checks passed!
compileall: exit 0
```

证据文件：`docs/verification/core-stabilization.md`。

### 5.2 FastAPI 后端 Task 1 已完成

已有：

- `backend/main.py`
- `backend/config.py`
- `GET /api/v1/health/live`
- CORS 基础配置。
- `backend/requirements.txt`
- `backend/.env.example`

### 5.3 FastAPI 后端 Task 2 已完成

已有 PostgreSQL/SQLAlchemy 模型：

- `analysis_jobs`
- `product_snapshots`
- `job_products`
- `artifacts`

已有：

- Async session。
- JobRepository create/get/transition/fail 基础方法。
- queued 到 running 的原子状态转换。
- Alembic 初始迁移。
- PostgreSQL 方言离线 SQL 生成验证。

### 5.4 FastAPI 后端 Task 3 已完成

已有严格 API Schema：

- `HypothesisJobCreate`
- `JudgmentJobCreate`
- `BatchJobCreate`
- `JobSummary`
- `JobDetail`
- `JobListResponse`
- `ArtifactResponse`

每个产品 URL 都复用核心 hostname 校验。

### 5.5 FastAPI 后端 Task 4 已完成

已有：

- `JobQueue` Protocol。
- `ArqJobQueue`。
- JobService 先写 PostgreSQL，再入 Redis 队列。
- Redis 入队失败时把任务标为 `QUEUE_UNAVAILABLE`。
- API 返回可重试的服务不可用错误。

注意：ARQ 当前配套 `redis>=5.2,<6.0` Python 客户端，这是依赖兼容要求；它仍可连接 Redis 8 服务端。

Task 4 完成时的后端基线为 `79 passed`，Ruff clean。

## 6. 当前精确停点：后端 Task 5

当前 Task 5 目标：暴露任务创建、列表、详情、重试、结果和产物下载 API。

已写并保留的文件：

- `backend/api/dependencies.py`
- `backend/api/routes/__init__.py`
- `backend/api/routes/jobs.py`
- `backend/tests/test_jobs_api.py`
- `backend/api/router.py` 已挂载 jobs router。

目前已经实现的两个行为：

- `POST /api/v1/jobs/hypothesis` 返回 202。
- `GET /api/v1/jobs/{job_id}`，不存在返回 404。

新增测试已经运行，最新 RED 证据为：

```text
2 passed, 7 failed in 2.80s
```

7 个失败都符合预期，因为以下路由尚未实现：

- `POST /api/v1/jobs/judgment`
- `POST /api/v1/jobs/batch`
- `GET /api/v1/jobs`
- `POST /api/v1/jobs/{job_id}/retry`
- `GET /api/v1/jobs/{job_id}/result`
- `GET /api/v1/jobs/{job_id}/artifacts/{kind}`

### 6.1 Task 5 接手后的第一组动作

先复现 RED：

```powershell
Set-Location 'D:\Desktop\bundling-system\.worktrees\web-platform'
.venv\Scripts\python.exe -m pytest backend/tests/test_jobs_api.py -q
```

预期仍然是 2 通过、7 失败。若数量不同，先查看 `git status`，不要直接改测试来迁就实现。

### 6.2 先补齐缺失测试

当前测试还应补充以下安全/状态行为，然后先确认它们失败：

- retry 原任务不存在时返回 404。
- retry 原任务不是 failed 时返回 409。
- retry 成功时新任务 `retry_of_id` 指向原任务。
- artifact 数据库记录不存在时返回 404。
- artifact 注册路径逃逸 `artifact_dir` 时返回 404。
- artifact 路径位于目录内但文件不存在时返回 404。
- artifact `kind` 只允许固定的 `json` 和 `excel`。
- list 的 mode/status filter 确实传给 repository。
- page 最小值 1、page_size 范围 1-100。

### 6.3 实现 JobService

修改 `backend/application/job_service.py`：

1. 为 `JobWriter` Protocol 增加 `get()`，用于 retry 查原任务。
2. 提取私有 `_submit(mode, payload, retry_of_id=None)`。
3. `submit_hypothesis()` 调 `_submit('hypothesis', ...)`。
4. 增加 `submit_judgment()`。
5. 增加 `submit_batch()`。
6. 增加 `retry(job_id)`：
   - 原任务不存在，抛稳定的 Not Found 应用错误。
   - 只有 `failed` 可重试，否则抛 Conflict 应用错误。
   - 使用原任务 mode 和 request_payload 创建新任务。
   - 新任务写入 `retry_of_id=原任务ID`。
7. 保持“数据库创建成功后才入 Redis”的顺序。
8. 保持入队失败时持久化 `QUEUE_UNAVAILABLE`。

不要在 Service 中创建浏览器、LLM 或 FastAPI 对象。

### 6.4 实现 JobRepository

修改 `backend/db/repositories.py`：

- 增加 `list(page, page_size, mode=None, status=None)`。
- 使用 SQLAlchemy `select()` 和 `func.count()`。
- mode/status 条件必须同时影响 items 和 total。
- 使用稳定排序，例如 `created_at DESC, id DESC`。
- 使用 offset `(page - 1) * page_size` 和 limit。
- 返回 `(items, total)`。
- 增加 `get_artifact(job_id, kind)`，查询 `Artifact.job_id` 和 `Artifact.kind`。

需要给 repository 的真实查询增加单元测试，不能只依赖 FastAPI 中的 AsyncMock。

### 6.5 实现路由

修改 `backend/api/routes/jobs.py`：

- 三种提交接口统一处理 Service 错误。
- `GET /jobs` 的静态路径必须避免被 `/{job_id}` 抢占。
- 列表返回 `JobListResponse(items, total, page, page_size)`。
- retry 返回 202 和新任务摘要。
- result：不存在 404，未完成 409，完成后返回 result_payload。
- artifact：只通过 `(job_id, kind)` 数据库记录获取路径，不接收任意文件路径。

产物安全检查必须同时满足：

1. `artifact.path` 执行 `Path.resolve()`。
2. `settings.artifact_dir` 执行 `Path.resolve()`。
3. resolved artifact 必须位于 resolved artifact_dir 内。
4. 必须是存在的普通文件。
5. `json` 使用固定 media type `application/json`。
6. `excel` 使用固定 media type `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`。
7. 下载文件名由服务端固定规则生成，不能使用用户输入拼接任意路径。

### 6.6 Task 5 验证和提交

先跑定向验证：

```powershell
.venv\Scripts\python.exe -m pytest backend/tests/test_jobs_api.py backend/tests/test_job_service.py backend/tests/test_job_repository.py -q
.venv\Scripts\ruff.exe check backend
```

再检查差异：

```powershell
git status --short
git diff --check
git diff
```

全部通过后，勾选 `docs/superpowers/plans/2026-07-15-fastapi-backend.md` 的 Task 5 三个步骤，再提交：

```powershell
git add backend/api backend/application/job_service.py backend/db/repositories.py backend/tests docs/superpowers/plans/2026-07-15-fastapi-backend.md
git commit -m 'feat: expose analysis job APIs'
```

提交后运行：

```powershell
git status --short --branch
git log -1 --oneline
```

工作树应为空，最新提交信息应为 `feat: expose analysis job APIs`。

## 7. 后端剩余阶段

严格以 `docs/superpowers/plans/2026-07-15-fastapi-backend.md` 为准，下面是执行提示。

### 7.1 Task 6：核心分析适配器和 ARQ Worker

先创建测试：

- `backend/tests/test_analysis_runner.py`
- `backend/tests/test_worker_jobs.py`

覆盖：

- hypothesis、judgment、batch 三种 mode 正确分派。
- 浏览器在成功和失败后都停止。
- 进度单调，例如 `5, 20, 55, 85, 100`。
- Worker 通过原子 transition 抢占任务。
- 已被抢占或不是 queued 的任务不会重复执行。
- 完成时持久化 result 和 artifacts。
- 已知异常映射为稳定 error code。
- 未知异常不向用户暴露堆栈、密钥和本地路径。
- `WorkerSettings.max_jobs = 1`，避免多个任务共享 Chrome profile。

实现文件：

- `backend/application/analysis_runner.py`
- `backend/workers/jobs.py`
- `backend/workers/settings.py`

验证：

```powershell
.venv\Scripts\python.exe -m pytest backend/tests/test_analysis_runner.py backend/tests/test_worker_jobs.py -q
.venv\Scripts\ruff.exe check backend
```

提交：

```powershell
git add backend/application/analysis_runner.py backend/workers backend/tests
git commit -m 'feat: execute analysis jobs in ARQ worker'
```

### 7.2 Task 7：Readiness、日志和安全边界

实现：

- `GET /api/v1/health/ready`。
- PostgreSQL、Redis、Worker 三项独立状态。
- 每个 probe 最多 2 秒。
- request ID 和结构化日志。
- 全局异常处理返回稳定 `{code, message, retryable}`。
- CORS 只允许配置中的前端 origin。
- 响应不得包含 API key、provider 原始 payload、堆栈和本地路径。

测试：

- `backend/tests/test_readiness.py`
- `backend/tests/test_security.py`

验证和提交：

```powershell
.venv\Scripts\python.exe -m pytest backend/tests/test_readiness.py backend/tests/test_security.py -q
.venv\Scripts\ruff.exe check backend
git add backend
git commit -m 'feat: harden backend health and error handling'
```

### 7.3 Task 8：本地基础设施和后端验收

创建：

- `docker-compose.yml`
- `backend/README.md`
- `docs/verification/backend.md`

Docker Compose 要求：

- PostgreSQL 17。
- Redis 8。
- named volumes。
- healthcheck。
- 端口只绑定 `127.0.0.1`。
- 本地默认账号可写在 example/compose 默认值中，真实密码只放未提交 `.env`。

建议本地环境：

```powershell
Copy-Item backend/.env.example backend/.env
docker compose up -d postgres redis
docker compose ps
.venv\Scripts\alembic.exe -c backend/migrations/alembic.ini upgrade head
```

后端阶段全量验证：

```powershell
docker compose config
.venv\Scripts\python.exe -m pytest tests backend/tests -q
.venv\Scripts\ruff.exe check app tests backend
.venv\Scripts\python.exe -m compileall -q app backend tests
```

把命令、日期、测试数量、exit code 和外部服务限制记录到 `docs/verification/backend.md`，然后提交：

```powershell
git add docker-compose.yml backend/README.md docs/verification/backend.md docs/superpowers/plans/2026-07-15-fastapi-backend.md
git commit -m 'chore: document and verify backend stack'
```

只有真实 PostgreSQL migration、Redis enqueue/dequeue、readiness 和 mocked worker completion 都验证后，才能开始前端 API 联调。

## 8. Next.js 前端执行顺序

完整细节位于：`docs/superpowers/plans/2026-07-15-nextjs-frontend.md`。

设计必须遵循已批准布局：`docs/design/bundling-dashboard-layout.png`。

视觉约束：

- 简洁、安静、工作台风格，不做营销落地页。
- Desktop 左侧 232px 导航，中间主操作区，右侧约 412px 状态区。
- Tablet 折叠侧栏，状态区移到表单下。
- Mobile 顶部导航、纵向堆叠、无横向溢出。
- 白色、石墨、冷灰为主；绿色只用于成功/就绪，黄/红用于警告/失败。
- 不使用渐变、装饰光球、大圆角卡片或卡片套卡片。
- radius 约 6px，letter-spacing 必须为 0。
- 图标优先使用 Lucide；图标按钮必须有 tooltip 和 accessible name。

### 8.1 前端 Task 1：脚手架

创建 `frontend/`，使用 App Router、TypeScript、Tailwind、shadcn/ui、Vitest、Testing Library、MSW、Playwright。

安装依赖按前端计划执行。网络下载需要用户批准时再申请，不要绕过。

验收：

```powershell
npm --prefix frontend test -- --run
npm --prefix frontend run typecheck
```

提交：`feat: scaffold Next.js operations frontend`。

### 8.2 前端 Task 2：响应式应用壳

实现 sidebar、mobile nav、service status 和页面 shell。验收导航语义、桌面/移动布局和键盘可达性。

提交：`feat: add responsive operations shell`。

### 8.3 前端 Task 3：类型化 API Client

实现 `apiFetch<T>`、稳定 `ApiError`、job types、query keys 和 polling interval。不得把后端 secrets 放入 `NEXT_PUBLIC_*`。

提交：`feat: add typed backend API client`。

### 8.4 前端 Task 4：Hypothesis/Judgment 表单

使用 React Hook Form + Zod，覆盖 URL 安全校验、B URL 增删、提交中禁用和成功导航。

提交：`feat: submit hypothesis and judgment jobs`。

### 8.5 前端 Task 5：任务状态和结果

覆盖 queued、running、completed、failed、404、retry、JSON/Excel 下载。任务终态后停止 polling。

提交：`feat: display live job progress and results`。

### 8.6 前端 Task 6：Batch 和历史记录

覆盖换行 URL 解析、去重、无效行、分页、mode/status filter、retry、空状态和行导航。

提交：`feat: add batch jobs and task history`。

### 8.7 前端 Task 7：视觉和无障碍

检查 label、focus、tab order、状态非纯颜色表达、320px 不溢出、固定控件高度和布局稳定性。

验证：

```powershell
npm --prefix frontend test -- --run
npm --prefix frontend run typecheck
npm --prefix frontend run lint
```

提交：`style: match approved operations workbench`。

### 8.8 前端 Task 8：Playwright 和文档

使用 API mock 做确定性 E2E，至少覆盖：

- Desktop 1440x900。
- Mobile 390x844。
- hypothesis submit。
- judgment B URL 管理。
- running 到 completed。
- failure retry。
- history filters。
- artifacts download。

验证：

```powershell
npm --prefix frontend test -- --run
npm --prefix frontend run typecheck
npm --prefix frontend run build
npm --prefix frontend exec playwright test
```

检查浏览器 console：无 hydration warning、资源失败、未处理 Promise 和运行时错误。

提交：`test: verify frontend workflows and responsive layout`。

## 9. 最终集成与启动

### 9.1 环境文件

从 example 创建本地配置，但不要提交：

```powershell
Copy-Item .env.example .env
Copy-Item backend/.env.example backend/.env
Copy-Item frontend/.env.example frontend/.env.local
```

后端默认连接：

```text
DATABASE_URL=postgresql+asyncpg://bundling:bundling@127.0.0.1:5432/bundling
REDIS_URL=redis://127.0.0.1:6379/0
CORS_ORIGINS=["http://localhost:3000"]
ARTIFACT_DIR=output/bundling
API_PREFIX=/api/v1
```

### 9.2 推荐启动顺序

终端 1：

```powershell
docker compose up postgres redis
```

终端 2：

```powershell
.venv\Scripts\alembic.exe -c backend/migrations/alembic.ini upgrade head
.venv\Scripts\python.exe -m uvicorn backend.main:app --host 127.0.0.1 --port 8000 --reload
```

终端 3：

```powershell
.venv\Scripts\arq.exe backend.workers.settings.WorkerSettings
```

终端 4：

```powershell
npm --prefix frontend run dev
```

访问：

- 前端：`http://localhost:3000`
- 后端：`http://localhost:8000`
- API Docs：`http://localhost:8000/docs`
- Liveness：`http://localhost:8000/api/v1/health/live`
- Readiness：`http://localhost:8000/api/v1/health/ready`

### 9.3 最终验收命令

```powershell
.venv\Scripts\python.exe -m pytest tests backend/tests -q
.venv\Scripts\ruff.exe check app tests backend
.venv\Scripts\python.exe -m compileall -q app backend tests
npm --prefix frontend test -- --run
npm --prefix frontend run typecheck
npm --prefix frontend run lint
npm --prefix frontend run build
npm --prefix frontend exec playwright test
docker compose config
```

然后执行一次本地 smoke test：

1. 提交一个安全或 mocked hypothesis job。
2. 确认 API 立即返回 202。
3. 确认 PostgreSQL 中存在 queued job。
4. 确认 Worker 抢占后状态变 running。
5. 确认 progress 单调上升。
6. 确认最终状态 completed。
7. 打开结果页面。
8. 下载 JSON 和 Excel。
9. 确认历史页面可找到任务。
10. 创建一个可控失败任务并验证 retry 生成新 job 且保留 lineage。
11. 再运行一次 CLI smoke test，确认 Web 改造没有破坏 CLI。

## 10. 常见问题处理

### 10.1 Python 在受限沙箱直接退出

已观察到某些受限执行环境下 Python 无输出并以 Windows 异常码退出。先在普通 PowerShell 中运行同一命令；不要因此修改业务代码。使用当前虚拟环境中的可执行文件：

```powershell
.venv\Scripts\python.exe -m pytest ...
```

### 10.2 Git 提交提示 `.git/worktrees` 无写权限

代码工作树可写，但某些代理沙箱不允许写 Git metadata。需要为具体 `git add`/`git commit` 请求受控授权，或在普通 PowerShell 中提交。不要用复制仓库或 reset 来绕过。

### 10.3 PostgreSQL 测试失败

检查：

```powershell
docker compose ps
docker compose logs postgres
.venv\Scripts\alembic.exe -c backend/migrations/alembic.ini current
```

确认使用 `postgresql+asyncpg://`，数据库存在，migration 已到 head。

### 10.4 Redis/ARQ 入队失败

检查：

```powershell
docker compose logs redis
docker compose exec redis redis-cli ping
```

预期 `PONG`。Python Redis 客户端保持 `<6.0`，不要仅因为 Redis 服务端是 8 就升级到 redis-py 6。

### 10.5 Walmart CAPTCHA 或浏览器失败

- 真实抓取需要 Playwright/Chrome 环境。
- 单元测试和 E2E 默认使用 fake/mock，不应依赖真实 Walmart。
- Worker 并发保持 1。
- 浏览器清理必须放 `finally`。
- 不要杀死用户自己启动且不属于管理器的 Chrome。

### 10.6 中文乱码

现有部分旧 README 输出有编码历史问题。新文件统一 UTF-8。PowerShell 读取可使用：

```powershell
Get-Content -Raw -Encoding utf8 '文件路径'
```

不要在没有确认原始编码时做全仓库机械转码。

## 11. 每个 Task 的完成定义

只有同时满足以下条件才可勾选完成：

- 测试在实现前真实失败过，且失败原因是功能缺失。
- 新实现对应的定向测试通过。
- 相关旧测试通过，没有回归。
- Ruff/TypeScript/lint 对当前范围无错误。
- `git diff --check` 无 whitespace error。
- 代码差异只包含当前 Task。
- 规划文档 checkbox 已同步。
- 验证输出已记录。
- 当前 Task 已独立提交。
- `git status` 不含意外文件。

不要用“代码看起来没问题”替代验证输出。

## 12. 可直接交给下一位开发者或 AI 的启动提示词

```text
请在 D:\Desktop\bundling-system\.worktrees\web-platform 中继续项目。
先完整阅读：
1. 交接文档.md
2. docs/superpowers/plans/2026-07-15-bundling-platform-master-plan.md
3. docs/superpowers/plans/2026-07-15-fastapi-backend.md
4. docs/superpowers/specs/2026-07-15-web-platform-design.md

当前分支必须是 feature/web-platform，HEAD 应为 8eb6fef。
先运行 git status，保留 backend/api/router.py、backend/api/dependencies.py、backend/api/routes/、backend/tests/test_jobs_api.py 的未提交 Task 5 现场。
从后端 Task 5 继续，先复现 test_jobs_api.py 的 RED，再补齐 retry 安全测试、artifact 路径逃逸测试、repository 查询测试，然后实现最小代码。
每个 Task 必须 TDD、定向验证、Ruff、独立 commit。完成后端 Task 5-8 和后端阶段门后，再严格按 Next.js 前端计划和已批准布局图实现前端。
不得破坏 app/ CLI，不得把浏览器/LLM 放进 FastAPI 请求进程，不得提交 secrets，不得跳过最终全量验证。
```

## 13. 当前结论

项目没有丢失，也不需要推倒重来。核心稳定化和后端 Task 1-4 已经提交；后端 Task 5 已进入 TDD 的 RED 阶段，未提交测试和基础路由文件都保留在实施工作树。下一位接手者应从第 6 节开始，完成 Task 5 后按第 7、8、9 节依次推进。
